# Agent、项目与截图评测验收记录

2026-09-21：功能实现可在本地验收，最终要求仍未达成。

- 结构化聊天：六种 IM 皮肤、iOS/Android/桌面外观、富消息、人物及背景编辑、普通/长 PNG 导出。
- 项目：规则、默认平台、作品归属、批量队列、取消、重试、会话撤销、崩溃恢复与原子发布。单个作品也可选择项目规则。
- 原截图：与评测共用 Agent 工具循环、编辑层契约、HTML、文字排版和 PNG 渲染。保留原图模式不声称自动拆解所有界面元素。
- 私有数据：11 张来源图及参考素材以加密包存 Git；本地解密、原图、模型输出与人工标注留在忽略目录。候选参考图必须人工审核才能成为金标。

## 最新验收：runtime v9（2026-09-21 16:35 Asia/Shanghai）

- 11/11 真实 DeepSeek 执行完成，6/11 严格自动通过，0 缓存复用。通过 C03、C04、C05、C06、C07、C10；C01、C02、C08、C09、C11 未通过。
- 已逐张检查本轮 PNG；C09 仍有图标与文字碰撞、气泡补丁边界和俄语语法问题。其他失败还包括编辑层分段、头像尺寸、标题区域和空格匹配。不能把所有失败都解释为评分误报。
- 原答案与评分阈值保持；人工金标未自动批准。私有逐例证据和源码指纹在本地验证目录，公开文档不包含原聊天。
- 本地 248 项核心测试、192 项评测测试和构建通过；45 项浏览器场景通过，后续渲染边界修复另跑对应 PNG 导出场景通过。独立 Pi 复核已关闭新发现的擦除/绑定回归，纹理背景恢复仍有明确局限。
- 全量结束后追加了旧版本已有大擦除层的兼容修复，单独及核心套件回归通过；全量数据集均从空编辑层启动，不进入该兼容分支。没有为该小修再次调用完整模型数据集。
- 另有真实 Web 创作、生图、选中图片参考编辑和导出证据。固定素材截图评测不等于每例都现场调用生图服务。

结构化创作的平台样式由代码模板维护；原图编辑由源图元素和槽位工具约束。两种模式的能力和未完成边界见 [Agent fidelity 架构](agent-fidelity-design.md)。

## 较早结果（runtime v9 之前）

`screenshot-agent-v5` / `deepseek-flash` 全量执行 11 例：**3/11 自动通过**，通过 C01、C03、C10。10 例生成 PNG，C07 因轮次超限无完成输出。全部保留真实工具轨迹。没有降低几何、语义或像素保护门槛。

失败涉及图片槽位边界不精确、遗漏重复文本、把一个要求拆成多个不匹配的编辑层和长文本区域覆盖。现有准备好的授权图片用于 placement；这不是三方生图 API 的端到端验证。

实际 DeepSeek 项目批量调用能创建并保存微信/WhatsApp 作品，但固定文案规则曾被扩写，重复运行仍有不稳定表现。任务完成状态与规则语义评测不能混为一谈。

## 未完成边界

1. 11/11 验收未通过。
2. 腾讯云 TokenHub WAND-Vega 已配置到本地服务及 GitHub Secret/变量。真实 DeepSeek 工具调用已完成生成与参考图编辑，且验收校验第二轮发送的原图字节与第一轮输出完全一致。
3. GitHub 已有 DeepSeek 与加密数据集 Secret。云端 CI 曾被账号 billing lock 阻止启动；需按最新提交读回，不能用本地绿灯替代 CI。
4. 跨系统字体存在差异；共用渲染实现不意味着 macOS 与 Linux 的字体像素必然相同。

工作流对可信分支解密数据、执行同一 Agent、导出纯指标并清除该作业的明文。失败返回非零状态，不自动批准金标。

较早本地验证：208 项核心测试、178 项评测工具测试通过；浏览器完成账号、项目、编辑、导出与响应式回归。CI 新增真实生图与参考图编辑 smoke gate，缺图片配置返回 `image_provider_not_configured`，不能用准备好的素材替代该门禁。

## 腾讯云凭据修复（2026-09-21）

Notion 旧记录混淆了 Key ID 与 API Key；通过控制台核对后修正索引，凭据保留在受限服务端环境文件。文生图与参考图编辑均使用异步提交、轮询和有界下载，不把任务受理写成成功。实际返回的 COS HTTP 地址先校验域名再升级 HTTPS，下载不携带 API Key；不跟随重定向。测试覆盖超时、取消、错误状态、响应大小、URL 校验和图片解码。
